spring:
  application:
    name: test-reader
  kafka:
    consumer:
      group-id: ${spring.application.name}
  flyway:
    locations:
      - classpath:db/migration
    callback-locations:
      - classpath:db/callback

topics:
  definitions:
    event:
     topic: event
     dlt: event-dlt
